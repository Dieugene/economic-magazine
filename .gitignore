# IDE
.idea/
.vscode/

# Development tooling (local only)
.agents/
.claude/
.cowork/
01_tasks/
AGENTS.md
DEVELOPER_WORKFLOW.md
CLAUDE.md
CLAUDE_CONTEXT.md
CLAUDE_HANDOFF.md

# Local working notes (may contain credentials)
00_docs/specs/backlog.txt

# Temporary/old files
.old/
*.bak

# Git worktrees (project-specific work)
.worktrees/

# Agent pool inbox (coordination payloads — live in Tasks API + local FS, не в git)
.inbox/

# Project data and logs (generated during work)
03_data/
04_logs/

# QA fixtures (binary, regenerable) — track only the manifest
00_docs/qa/fixtures/*
!00_docs/qa/fixtures/README.md

# OS
.DS_Store
Thumbs.db

# Git
*.orig

# agent long-term memory: local only, never leaves this machine
# (each role keeps its own git repo inside .memory\<role>\)
.memory/
